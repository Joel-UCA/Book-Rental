# JWT Configuration
# Generate a strong secret key (minimum 64 characters recommended)
JWT__SECRET=your-super-secret-jwt-key-here-minimum-64-characters-recommended
JWT__ISSUER=BookRentalApp
JWT__AUDIENCE=BookRentalUsers
JWT__EXPIRATIONHOURS=1

# Admin User Configuration
# These credentials will be used to create the initial admin user
ADMINUSER__EMAIL=admin@bookrental.com
ADMINUSER__PASSWORD=your-strong-admin-password-here
ADMINUSER__FULLNAME=System Administrator

# Database Configuration
CONNECTIONSTRINGS__DEFAULTCONNECTION=Server=(localdb)\\mssqllocaldb;Database=BookRentalDb;Trusted_Connection=True;MultipleActiveResultSets=true

# Instructions:
# 1. Copy this file to .env (do not commit .env to version control)
# 2. Replace all placeholder values with actual configuration
# 3. For production, use environment variables or Azure Key Vault instead of .env file
# 4. Ensure JWT__SECRET is at least 64 characters long for security
# 5. Use a strong password for ADMINUSER__PASSWORD (minimum 8 characters, with uppercase, lowercase, numbers, and special characters)
